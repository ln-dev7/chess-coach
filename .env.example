# AI coach (the "✦ AI coach lesson" button). Get a key at https://console.anthropic.com
# Without it, the app still works — template lessons, puzzles and analysis are fully local.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model used by the coach (default: claude-sonnet-4-6)
# ANTHROPIC_MODEL=claude-sonnet-4-6
